[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[build.processing]
  skip_processing = true

[[edge_functions]]
  path = "/*"
  function = "strip-trailing-slash"
